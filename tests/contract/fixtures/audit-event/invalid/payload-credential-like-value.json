{
  "event_hash": "sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
  "prev_event_hash": null,
  "action": "system.config.updated.v1",
  "recorded_at": "2026-07-12T17:10:00Z",
  "scope": "system",
  "trace_id": "22222222222222222222222222222222",
  "payload": {
    "config_line": "password=example-not-real"
  },
  "_note": "PERMANENT gap fixture (non-expiring, tests/contract/README.md fixture metadata convention). ADR-0015:64 'AuditEvent 에러 기록 범위' + this schema's own payload $comment forbid PII/secret content in payload, but this is a RUNTIME gate + review obligation, NOT schema-enforceable content inspection -- JSON Schema cannot semantically detect that a free-form payload object's string value looks like a credential. This instance is schema-VALID despite containing a credential-shaped string; the value used ('password=example-not-real') is a deliberately FAKE, low-entropy, non-functional placeholder per ADR-0020 fixture policy (never a real/high-entropy secret-shaped value) so it is also gitleaks-safe. Do not 'fix' this fixture into schema-detectable invalidity -- it exists specifically to document/lock the gap the payload's open shape leaves."
}
