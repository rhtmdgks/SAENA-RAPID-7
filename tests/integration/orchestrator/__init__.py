"""Integration tests for saena_orchestrator against a REAL Temporal
workflow, run via `temporalio.testing.WorkflowEnvironment.start_time_skipping`
(a real embedded Temporal test-server process, not a mock)."""
