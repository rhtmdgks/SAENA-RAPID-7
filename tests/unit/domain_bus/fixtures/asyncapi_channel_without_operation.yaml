asyncapi: 3.0.0
info:
  title: SAENA events (w2-18 unit-test malformed fixture)
  version: "1.0.0"
  description: >-
    Test-only fixture -- a channel with no matching operations[] entry, so
    no producer is derivable for it. Exercises
    saena_domain.bus._envelope_check.TopicCatalogError
    ("no matching operation") -- the SHOULD-FIX missing_producer post-parse
    check added in the w2-18 critic review.
defaultContentType: application/json
channels:
  patch.unit.completed.v1:
    address: patch.unit.completed.v1
    messages:
      patchUnitCompleted:
        name: PatchUnitCompleted
        title: patch.unit.completed.v1
        summary: Patch unit completed (agent-runner-service).
        contentType: application/json
        payload:
          schemaFormat: "application/schema+json;version=draft-2020-12"
          schema:
            type: object

operations: {}
