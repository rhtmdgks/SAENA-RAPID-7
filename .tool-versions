# mise/asdf fallback for local (non-devcontainer) development — ADR-0022.
# Sync duty: these versions MUST match the ARG defaults in .devcontainer/Dockerfile
# exactly (drift = ADR-0022 violation). Whoever bumps one MUST bump the other in
# the same patch unit. Exact patch versions below were chosen at authoring time
# (2026-07-12, T14) and are not "latest" floats — re-pin deliberately.

python 3.12.8
uv 0.10.4
just 1.56.0
kubectl 1.31.4
helm 3.16.4
k3d 5.7.5
nodejs 22.12.0
oasdiff 1.11.6
gitleaks 8.30.0
