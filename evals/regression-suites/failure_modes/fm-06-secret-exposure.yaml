mode_id: fm-06-secret-exposure
k3s_section: "10"
name: "Secret exposure"
example: ".env referenced in source"
expected_system_behavior: "redaction and stop"
status: covered
covering_axis: regression_suite_native
covering_test: tests/unit/evals_harness/test_failure_mode_regression_suite.py::test_secret_exposure_redacted_in_handoff_detail
notes: >
  Not one of the 9 mandatory eval axes (mission scope) — a dedicated
  regression-suite check over REAL saena_hooks_runtime.redact.
  redact_patterns, exercised through before_handoff (the SAME redaction
  call handoff_completeness's own hard-block finding strings go through):
  a finding string referencing ".env"-shaped secret material is redacted
  before it ever appears in a HookDecision.detail.
