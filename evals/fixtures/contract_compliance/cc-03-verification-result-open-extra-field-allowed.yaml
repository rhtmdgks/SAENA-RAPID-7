fixture_id: cc-03-verification-result-open-extra-field-allowed
axis: contract_compliance
seed: 2003
tag: false_positive_guard
description: >
  domain/verification-result/v1 is the ONE open-class domain contract
  (additionalProperties intentionally unset at the root, per the schema's
  own $comment). A payload carrying an extra, unknown top-level field looks
  like it should fail a naive "closed schema" assumption, but the contract
  is deliberately open — this MUST validate successfully.
expected_passed: true
expected_score: 1.0
threshold: 1.0
input:
  schema_relpath: domain/verification-result/v1/verification-result.schema.json
  payload:
    tenant_id: acme-co
    run_id: run-0001
    patch_unit_id: PU-01
    gate_id: build
    status: passed
    worktree_commit: abc1234
    evaluated_at: "2026-07-13T00:00:00Z"
    gate_specific_forward_compat_metadata:
      builder_version: "1.2.3"
